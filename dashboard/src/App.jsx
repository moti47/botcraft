/**
 * BotCraft Dashboard — Main Entry Point
 *
 * Replaces the Tailwind-based dashboard with the BotCraft design system.
 * Uses Supabase for all data and Vercel Edge Functions for mutations.
 */

import BotCraftPage from './BotCraftPage'

export default function App() {
  return <BotCraftPage />
}
