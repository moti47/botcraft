[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: tests that hit external services (skip with -m "not integration")
    smoke: lightweight smoke tests (run with -m smoke)
filterwarnings =
    ignore::DeprecationWarning
