/**
 * Shared pipeline utilities
 * (placeholder for future shared code)
 */

export async function videoPipeline(videoId: string): Promise<any> {
  // Placeholder: full pipeline logic would go here
  return { video_id: videoId, status: "processing" };
}
